{
  "name": "@truerate/extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@truerate/core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@truerate/harness": "workspace:*",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "wxt": "^0.19.13"
  }
}
